import random

start=random.choices(range(1,5))
if start == [1]:
    ball.dx *= -1
if start == [2]:
    ball.dy *= -1
if start == [3]:
    ball.dx *= -1
    ball.dy *= -1
if start == [4]:
    pass



    ball.dx *= -1
    ball.dy *= -1
